'use client'; import {motion} from 'framer-motion'; import {ArrowRight,CheckCircle2} from 'lucide-react';
export function Button({children,...p}:React.ButtonHTMLAttributes<HTMLButtonElement>){return <button {...p} className={'rounded-xl bg-sage px-5 py-3 font-semibold text-ink transition hover:scale-[1.02] hover:shadow-glow disabled:opacity-50 '+(p.className||'')}>{children}</button>}
export function Card({children,className='' }:{children:React.ReactNode,className?:string}){return <motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} className={'glass rounded-2xl p-6 '+className}>{children}</motion.div>}
export function Nav({admin=false}:{admin?:boolean}){return <nav className="sticky top-0 z-50 border-b border-white/10 bg-ink/80 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4"><a href="/" className="font-bold tracking-tight">digital<span className="text-sage">heroes</span></a><div className="hidden gap-6 text-sm text-muted md:flex"><a href="/charities">Charities</a><a href="/#how">How it works</a>{admin&&<a href="/admin">Admin</a>}</div><div className="flex gap-2"><a href="/login" className="rounded-xl px-4 py-2 text-sm">Log in</a><a href="/signup" className="rounded-xl bg-sage px-4 py-2 text-sm font-semibold text-ink">Subscribe <ArrowRight className="inline" size={15}/></a></div></div></nav>}
export function Stat({label,value}:{label:string,value:string}){return <div><div className="text-2xl font-bold">{value}</div><div className="text-xs text-muted">{label}</div></div>}
export function Check({children}:{children:React.ReactNode}){return <li className="flex gap-2 text-muted"><CheckCircle2 size={18} className="mt-0.5 text-sage"/>{children}</li>}
