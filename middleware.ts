import {NextResponse,type NextRequest} from 'next/server';import {createServerClient} from '@supabase/ssr';
export async function middleware(req:NextRequest){let res=NextResponse.next({request:req});const supabase=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,{cookies:{getAll(){return req.cookies.getAll()},setAll(cs){cs.forEach(({name,value})=>{req.cookies.set(name,value);res.cookies.set(name,value)})}}});const {data:{user}}=await supabase.auth.getUser();if(req.nextUrl.pathname.startsWith('/dashboard')||req.nextUrl.pathname.startsWith('/verify')||req.nextUrl.pathname.startsWith('/admin')){if(!user)return NextResponse.redirect(new URL('/login',req.url));const {data:p}=await supabase.from('profiles').select('subscription_status').eq('id',user.id).single();if(req.nextUrl.pathname.startsWith('/admin')){if(user.email!=='admin@test.com')return NextResponse.redirect(new URL('/dashboard',req.url))}else if(p?.subscription_status!=='active')return NextResponse.redirect(new URL('/signup?reason=subscription',req.url))}return res}
export const config={matcher:['/dashboard/:path*','/verify/:path*','/admin/:path*']};
