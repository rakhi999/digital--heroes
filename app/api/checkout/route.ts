import {NextResponse} from 'next/server';import Stripe from 'stripe';import {supabaseServer} from '@/lib/supabase-server';
export async function POST(req:Request){const {plan}=await req.json();const s=supabaseServer();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});if(!process.env.STRIPE_SECRET_KEY){await s.from('profiles').update({subscription_status:'active',plan}).eq('id',user.id);return NextResponse.json({url:'/dashboard?mock=1'});}const stripe=new Stripe(process.env.STRIPE_SECRET_KEY);const price=plan==='yearly'?process.env.STRIPE_YEARLY_PRICE_ID:process.env.STRIPE_MONTHLY_PRICE_ID;const session=await stripe.checkout.sessions.create({mode:'subscription',line_items:[{price:price!,quantity:1}],customer_email:user.email!,success_url:`${process.env.NEXT_PUBLIC_APP_URL}/api/checkout/success?session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${process.env.NEXT_PUBLIC_APP_URL}/signup?cancelled=1`,metadata:{user_id:user.id,plan}});return NextResponse.json({url:session.url})}
